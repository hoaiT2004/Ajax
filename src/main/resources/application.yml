server:
  port: 8080

spring:
  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  datasource:
    url: 'jdbc:mysql://localhost:3306/UseAjax?createDatabaseIfNotExist=true'
    username: root
    password: 24052004